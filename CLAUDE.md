# Internal Phishing Simulation & Security Awareness Training Platform

Internal, self-hosted defensive security tool: runs simulated phishing campaigns against company employees, auto-assigns training on failure, and reports on organizational risk. **Not** a commercial product, not for actual malicious use. Full architecture rationale lives in [phishing-training-platform-plan.md](phishing-training-platform-plan.md) — read it for anything not covered below.

**Status**: Phase 0 infra portion complete and verified (domain/deliverability portion still pending real infra — see README.md). **Phase 1 done, bug-reviewed twice, fixed both times**: `Employee`/`Department`/`Campaign`/`Event`/`AuditLogEntry` models; a real `GophishClient` adapter; Gophish webhook ingestion with signature verification, DB-level idempotency, and defensive credential stripping; reconciliation scheduled via `CELERY_BEAT_SCHEDULE`; RBAC via `setup_groups` (Phase 1 shipped two groups; Phase 3 replaced them with five — see below); audit logging on campaign launch, employee/department CRUD, and CSV export. **Phase 2 (training loop) done, verified live, and bug-reviewed**: `TrainingModule`/`Quiz`/`QuizQuestion`/`QuizChoice`/`TrainingAssignment`/`QuizAttempt` models; auto-assignment on simulation failure via a `post_save` signal on `Event`, tested end-to-end through a real signed webhook POST; pure `score_quiz()` function; hourly reminder task with a 24h cooldown, verified live (sent once, correctly suppressed on retry) via the console email backend. A **second review pass** (after Phase 2 landed) found and fixed: `setup_groups` was never updated for the six new training models, so a non-superuser Admin couldn't touch training data at all — fixed, and now has a regression test (`test_admin_group_can_manage_training_models`) so it can't silently reappear. Training admins also didn't use `AuditedAdminMixin` unlike every other admin in the codebase — fixed for consistency, including explicit audit logging on the `mark_started` bulk action (bulk `.update()` bypasses `save_model`). A refactor pass preceded Phase 2: cross-app FKs converted to Django's lazy string form (avoids a real circular import that direct-import FKs would have hit: campaigns → training → events → campaigns), plus deduplication of the webhook/reconciliation transaction logic (`events/services.py::record_event`) and the admin permission-check pattern (`AuditedAdminMixin.require_permission`) — the pre-refactor test suite passed unmodified afterward, confirming no behavior change. 23/23 tests passing. **Third review pass** found one more: the local-dev browser-access file was initially named `docker-compose.override.yml`, which `docker compose up` auto-loads with no flag — meaning a real deployment running the plain standard command on a fresh clone would've silently exposed the admin on `:8000`, directly against invariant #8's "hard requirement, not hardening." Renamed to `docker-compose.dev.yml`, which requires an explicit `-f docker-compose.dev.yml` to load — see README "Viewing the admin UI in a browser." **Fourth pass — full completeness audit against the plan doc's phase checklists** (not just bug-hunting): confirmed every Phase 0–2 line item is either done-and-verified or explicitly, deliberately flagged as not done — see the completeness table kept in the session record / ask for a re-summary if needed. The one previously-unverified claim this pass closed: "campaign sending" (Phase 1) had only ever been tested against `FakePhishingEngineClient`, never a real Gophish instance. Verified for real: set up a live Gophish instance (real API key, template, landing page, sending profile, group, all via its REST API), ran `CampaignAdmin.launch_campaign` through the actual browser, and confirmed Gophish genuinely created and accepted the campaign (201, `status: "In progress"`, target queued) — `GophishClient.create_campaign()` works correctly end-to-end. Also incidentally confirmed invariant #4's password-stripping live: the landing page's `<input type="password">` came back from Gophish's own API with its `name` attribute stripped, exactly as documented. Found in the same pass: Gophish's mail worker can panic and crash the container on a bad sending profile (see "Known deployment quirks") — fixed by adding `restart: unless-stopped` to every long-running service in `docker-compose.yml` (none had a restart policy before). **Phase 3 (management/security hardening) done and verified live against real Gophish**: five RBAC groups (`Security Admin`, `Campaign Manager`, `Training Manager`, `Report Viewer`, `Department Manager`) replacing Admin/Viewer in place; Department Manager scoped row-level via `DepartmentScopedAdminMixin` + `Department.managers` (Django's permission framework is model-level only); campaign approval workflow (`draft → pending_approval → approved → launched`) with a custom `campaigns.approve_campaign` permission so a Campaign Manager provably cannot self-approve (checked in the browser); one shared `campaigns/services.py::launch_campaign()` used by both the admin action and the Celery Beat scheduler; **exemptions now actually enforced** — before Phase 3, `Employee.is_exempt` affected nothing and Django never synced employees into a Gophish group at all (only worked in earlier live tests because the group was hand-created); launch now syncs `department, is_exempt=False` into Gophish first (verified: exempt employee absent from the Gophish group); org-wide launch rate limit (`CAMPAIGN_LAUNCH_RATE_LIMIT`, default 5/24h, blocked attempts audit-logged); landing-page preview via `PhishingEngineClient.get_landing_page_html`; audit log admin gained date drill-down, actor filter, and Security-Admin-only CSV export. 39/39 tests. Known limits: the rate-limit check-then-launch is not atomic (two simultaneous launches could exceed the limit by one); anyone with `change_campaign` (incl. Department Manager, for their own department) can launch an *already-approved* campaign — approval is the gate, not a separate launch permission. **Phase 0-3 review pass (2026-09-24) found and fixed**: Campaign `status`/approval fields were editable in the admin edit form (a Campaign Manager could set Status=approved, defeating the approval gate) — now read-only, changed only by audited actions; `submit_for_approval` had no permission check (a Report Viewer could run it); editing template/landing page/target after approval kept the approval — now resets to Draft; Department Manager held `change_department` (could add themselves to any department's managers) and could move records into departments they don't manage — now view-only on Department and department choices are scoped; the landing-page preview served Gophish-hosted HTML/JS from the admin's origin — now `Content-Security-Policy: sandbox`; two copies of one campaign (admin click + scheduler) could both launch it — `launch_campaign` now row-locks and re-reads; a non-object webhook body 500'd instead of 400; `created_by` was never set. 54 tests. **Phase 4 (analytics & risk scoring) done and verified live** (signed webhook -> signal -> Celery worker -> snapshot -> API): `risk_scoring.RiskScoreSnapshot` (append-only, versioned, `contributing_metrics`), pure `scoring.compute_risk_score` (algorithm `v1`: worst outcome per campaign — submit 50 / click 25, report credit -15, +10 per repeat failed campaign, 180-day half-life decay, `email_opened` and all training events ignored), recompute on scoring-relevant events (post_save -> `on_commit` -> `recompute_score`) plus a daily `recompute_all_scores` (decay moves scores with no new events), `analytics.py` (campaign rates on distinct employees, training compliance kept separate from the score, department summary, improving/stagnant/worsening trend, weekly department trend), and staff-only JSON endpoints under `/analytics/` (department-scoped for Department Managers via `core.scoping`) that the Phase 4.1 dashboard will consume. To change the algorithm: add a version to `scoring.py`, then run `recompute_all_scores('<version>')` — old snapshots stay. **Phase 4.1 (custom frontend: Django templates + HTMX + Chart.js) is next, by user decision.** 98 tests. **Full Phase 0-4 review (2026-09-24) found and fixed**: auto/manual training assignments never got a `due_at`, so the Phase 4 `overdue` metric could never be non-zero (now `TRAINING_DUE_DAYS`, default 14); one undeliverable reminder address aborted the hourly reminder loop for everyone after it (now per-assignment try/except); `QuizAttempt.passed` was a free checkbox that could contradict the score (now derived from the quiz threshold); launching into a department with no eligible employees surfaced a raw Gophish 400 (now a clear `CampaignLaunchError`); anonymous `/analytics/` requests redirected to a nonexistent `/accounts/login/` (`LOGIN_URL` now the admin login); CSV exports were open to spreadsheet formula injection (`core.csv_safe`); gunicorn's 30s default timeout was shorter than three chained Gophish calls, which could kill a launch after Gophish had already sent (now 120s). Added an admin smoke test (every admin page x every role never 5xx). 115 tests. Known unverified: the webhook and reconciliation paths derive `external_id` from Gophish's `time` string and have only been proven equal in unit tests, not against real Gophish events (mail sending needs a working SMTP; without it Gophish crashes) — if the formats differ, duplicate events are possible but scoring and rates are per-campaign/distinct-employee so they stay correct. **Phase 4.1 (custom dashboard) done and verified in a real browser**: `apps/dashboard` — a read-only, server-rendered dashboard at `/` (Overview, Campaigns + detail funnel, Employees + detail with score history and score drivers, Departments + detail, Training), Django templates + HTMX (live search/filter/sort/pagination via `_partial.html` swaps) + Chart.js, both self-hosted under `static/dashboard/vendor/` (pinned htmx 2.0.4, Chart.js 4.4.7 — no CDN, VPN-only deployment). Design system in `static/dashboard/app.css` (tokens from the frontend-design skill, light + dark, reduced-motion aware, AA-contrast risk badges that always carry a text label). Strict CSP on dashboard pages only (`DashboardSecurityHeadersMiddleware`: no inline script/style, so templates have none); WhiteNoise serves static files. Same access rule and row scoping as the API (`dashboard/scope.py` -> `core.scoping`); risk levels and thresholds come from `risk_scoring.analytics` so UI and scoring can't drift. Login at `/login/` (`LOGIN_URL`), management still lives in `/admin/` (linked as "Manage"). Dev demo data: `python manage.py seed_demo_data` (DEBUG only; `--reset` removes it). Review found and fixed: HTMX sort was broken because the search form's `hx-include` was inherited by the sort links, and an empty `?sort=` bypassed the default order. 147 tests. **Phase 5 (reporting) done and verified live** (all six report types generated against real data; PDF rasterized and inspected; evidence-package checksums verified outside the container): `apps/reporting` — `GeneratedReport` (append-only, SHA-256, filename, scope, as-of, algorithm version), one entry point `reporting/services.py::generate_report()` (permission check, scoping, archive, audit `report_generated`; downloads audit `report_downloaded`), six kinds (executive summary PDF; campaign results, department summary, training compliance, employee risk scores CSV; evidence package ZIP with manifest + SHA256SUMS + methodology). **Historical reports are recomputed from the immutable event log as of the report date** (`risk_scoring.services.ScoreHistory`), never read from snapshots, so any past report is reproducible (PDF and ZIP are byte-deterministic). RBAC: `reporting.generate_report` (aggregate; Report Viewer, Campaign Manager, Department Manager scoped to their departments, Security Admin) and `reporting.export_employee_level` (names people: Security Admin only); Training Manager has neither. All CSV cells go through `csv_safe`, UTF-8 with BOM. A daily Celery task keeps last month's org-wide executive summary archived. Row scoping helpers moved to `core.scoping`. PDF limitation: ReportLab does not shape right-to-left scripts (CSV is lossless). 193 tests. **Full review through Phase 5 (2026-09-24, before the first push to GitHub)** found and fixed: pinned Django 5.1.4 (31 known vulnerabilities, out of support) and requests 2.32.3 (4) — upgraded to Django 5.2.17 LTS and requests 2.34.2 (all 193 tests pass unchanged; every other pin was clean per `pip-audit`); a missing `reporting` migration (help-text edited after the migration was generated — caught by `makemigrations --check`); production settings had no HSTS or proxy-SSL handling (now opt-in via `DJANGO_SECURE_HSTS_SECONDS` / `DJANGO_BEHIND_TLS_PROXY`, documented in `.env.example`); dead code and unused imports found by ruff; an N+1 query in the reports archive. **Phase 6 (adoption & program maturity) is built and tested (306 tests).** 6.1: employee training portal at `/portal/` (apps.portal) — employees are not Django users, they sign in with a signed, expiring **magic-link** token emailed to them (the plan's IdP-less fallback; admin MFA/SSO is still invariant #7's open item), see only their own assignments, take the quiz (scored by the existing `score_quiz()`), and get a certificate; plus a dashboard **campaign builder** and approval queue (apps.manage) on the one audited `launch_campaign()` path. 6.2: employee **CSV import** with deactivate-not-delete offboarding (new `Employee.is_active`); **report-a-phish** intake (apps.intake — portal form + mail-integration API) with a triage queue and a public teachable-moment page; a tagged **template catalog** and **smart (dynamic) audiences** (`campaigns.SmartGroup` + `audience.py`, exempt/inactive always filtered last); **mandatory-training policies** with manager escalation; emailed **scheduled reports** and a **read-only reporting API**. 6.3/6.4 (apps.engagement): coaching email at the moment of failure, **recognition points** that reward reporting (never punish failure), **SIEM/chat webhook** export, an ROI baseline-vs-current line, **privacy** controls (minimum-cohort suppression + PII anonymization that preserves the immutable event log), and an SPF/DMARC/DKIM **deliverability pre-flight**. **A read-and-draft API with scoped, hashed API keys** (apps.api, `/api/v1/`): a key acts as its owner and is limited to scope ∩ owner-permissions; it can draft training, email templates, landing pages and **draft** campaigns, but there is deliberately **no launch/approve/send/delete endpoint** — a person approves and launches every campaign in the UI, and `capture_passwords` is never accepted (invariant #4). The management UI (`/manage/`) is the new day-to-day surface; Django admin stays as the advanced one. **Phase 7 (production hardening: admin MFA/SSO, throttling, backups, monitoring, real SMTP relay, data governance) is now written into the plan doc.** **Code review + refactor pass (2026-09-24, after the first push)** found and fixed: the webhook 500'd (so Gophish redelivered forever) on an out-of-range timestamp, a non-string `time`, or a non-string `message` — `parse_datetime` *raises* for those rather than returning None — and the same input aborted a whole reconciliation run; webhook and reconciliation now share one `events/services.py::ingest_engine_event()` (employee lookup, time parsing, credential stripping, dedup). The `/analytics/` API had its own copy of department scoping that 404'd department-less employees/campaigns for org-wide roles — now uses `core.scoping` like the dashboard and reporting. Over-long audit descriptions (`str(TrainingAssignment)` can exceed 500 chars) and a Department Manager's report scope label (>200 chars with many departments) caused database errors — now truncated. The webhook tests depended on the deployment's `GOPHISH_WEBHOOK_SECRET` and failed outside Docker — now set by a fixture. 202 tests. **Second round + frontend review (same day)**: Gophish's admin API listened on 0.0.0.0 while the container is also on `public`, so nginx could reach it — now bound to a fixed internal-network IP (`GOPHISH_ADMIN_LISTEN`, README check 6; verified with stand-in containers on the real compose networks, not yet a full Gophish build). A launch that timed out after Gophish sent could send twice — Gophish campaigns are now named `<name> [#<pk>]` and adopted via `find_campaign`. New `employees.export_employee_data` permission (Security Admin only; **re-run `setup_groups` after deploying**); Department Managers can no longer set `is_exempt` or clear a department; rescheduling resets approval; reconciliation stops after `RECONCILE_WINDOW_DAYS`; reminders skip inactive modules; health check never 500s. Dashboard: the week-over-week delta now uses the shared `analytics.change_direction()` (TREND_DELTA) instead of calling any wobble improving/worsening; monotone chart interpolation (no invented peaks); card clipping and a dark-mode `--row-alt` token. Open items: `REQUIRES_ATTENTION.md`. 219 tests. **Phase 6 (adoption and program maturity) is now written into the plan doc, with a sourced competitive gap analysis.** Still not built: employee-facing training/quiz pages (needs the SSO decision), management actions in the dashboard (deliberately admin-only), campaign creation UI. **In-site slide courses (added after Phase 6)**: `TrainingSlide` (plain text, escaped by the `slide_body` filter) — the portal shows a module's slides (`/portal/training/<id>/course/?s=N`) and the last slide leads straight into the quiz (`/portal/training/<id>/quiz/`, needs the course opened first); `content_url` is now optional. Manage → Training has slide add/edit/reorder plus a staff preview of the course and quiz (records nothing) and an email-template preview; `seed_sample_content` loads a sample library (idempotent, launches nothing). 340 tests. **Email builder**: `campaigns.EmailDraft` holds the builder fields (layout, header/logo, heading, body, tracked button, footer); `email_render.render_email` compiles them to escaped, inline-styled, table-based email HTML and every save pushes it through the adapter (Manage → Content → edit, with a side-by-side preview). The button is Gophish's `{{.URL}}` so clicks are tracked like any link. **Image uploads + landing-page builder**: `EmailImage` uploads (PNG/JPEG/GIF only, identified by magic bytes, 1 MB, random names; SVG refused) are written to the `email-images` volume and served to recipients by nginx `/i/` from a READ-ONLY mount (no listing, nosniff; nginx still has no route to Django or the Gophish admin port); `EMAIL_IMAGE_BASE_URL` must be the public sending-domain URL in production. `LandingDraft` + `landing_render.render_landing` compile a native `<form>` with a real `type=password` input and no script (invariant #4) and push it with `capture_passwords` forced off; previews inline uploaded images as data URIs. **Composer + dropdown campaigns**: emails are written in an Outlook-style composer (contenteditable + small toolbar, `email_editor.js`); `campaigns.richtext.sanitize_email_html` rebuilds the message from an allow-list on save (every link becomes `{{.URL}}`, only library images, no script/style/forms, non-merge `{{ }}` defused). Campaigns are created from dropdowns (email, landing page, audience = department / smart group / everyone, training on failure). Manage is a separate admin-console shell (`manage/base.html`). 390 tests. **Mail settings** (Manage → Mail settings, Security Admin only): host, port, username, write-only password, return address and an allow-self-signed toggle are pushed through the adapter (`get/upsert_sending_profile`, `send_test_email`) to the engine's default sending profile; the password is never read back, shown or logged. Verified against real Gophish (create + blank-password update); the test-message send was NOT exercised. Django's own mail (portal links, reminders) is still env-configured, and Django sits on the `internal: true` network, so it cannot reach a LAN relay without a deliberate egress decision. 409 tests. **Dashboard GRC review (2026-09-24)**: every dashboard page gained the metrics an auditor or risk owner asks for, all computed in `risk_scoring/program_metrics.py` (pure reads over the event log; unit = one employee x one campaign; `email_opened` never read; small cohorts suppressed like the rest of analytics). Overview: reporting-period selector (30/90/180/365 days/all), programme health against configurable targets (`TARGET_MAX_FAILURE_RATE`, `TARGET_MIN_REPORT_RATE`, `TARGET_MIN_TRAINING_COMPLETION`, `TARGET_MIN_COVERAGE`, graded in words not colour), test coverage, reports-per-failure ratio, median time to click/report, per-campaign failure-vs-report chart, risk distribution, a needs-attention list (repeat failures, high risk with no training done, overdue, never tested, reports to triage). Campaigns: name/department/period filters, totals for the filtered set, template difficulty; detail adds response-speed curve, department breakdown, reporters, follow-up training and comparison with all campaigns. Employees: risk-level and focus filters, failed/reported/last-tested/training columns. Employee detail: per-campaign simulation history. Departments: failure/report/coverage/repeat/trend vs the organisation, sortable. Training: per-module completion and quiz stats, overdue ageing, on-time rate, department and module filters. Targets are policy, not measurements — set them to your risk appetite. **Full review (2026-09-24, after the composer/mail work)** found and fixed: the portal sign-in page emailed inline, so response time revealed whether an address was an employee (now a Celery task enqueued for every address, identical timing; `PORTAL_CLIENT_IP_HEADER` for a proxy so throttling isn't one shared bucket); deleting a library image that was placed inside a message body was allowed and would break already-sent emails; line breaks could be smuggled into the mail-settings return address/username; choosing 'Everyone' could 500 if duplicate groups existed; email/landing names could contain `/` (broke preview URLs) and could be renamed out from under campaigns; a training-form template had a duplicated block in its title. pip-audit clean, no CSP errors or horizontal overflow at phone width on 10 pages. 418 tests. Known and unchanged: portal magic links are reusable until they expire (72h default); quiz gating checks only that the course was opened. MFA is **explicitly out of scope for now** (invariant #7), not merely deferred. **Governance pass in the admin console (2026-09-24, GRC review of `/manage/`)**: new pages under a Governance heading — **Program & controls** (`core/governance.py`: live control-health checks, indicative ISO 27001 / SOC 2 / NIST 800-53 / PCI DSS / HIPAA / NCA ECC mapping, policy settings in force, read-only by design), **Audit log** (filter + Security-Admin-only CSV export), **Users & access** (roles, dormant accounts, access-review export and attestation; new `core.manage_user_access` permission, Security Admin only — **re-run `setup_groups` after deploying**; cannot edit yourself or superusers, cannot remove the last Security Admin), **Exemptions** (`Employee.exempt_reason/exempt_until/exempt_set_by/exempt_set_at`; a reason is required, expired exemptions stop protecting anyone at launch via `employees.exemptions.not_exempt_q` and are ended+audited hourly by `employees.tasks.lapse_expired_exemptions`), and **Training assignments** (extend a due date or waive with a mandatory reason; `TrainingAssignment.waived_*` + append-only `TrainingExtension`; a waived assignment leaves the completion denominator, is never counted as completed, is not reminded, and reports stay reproducible as-of via `ReportData._due_as_of`). Migrations: core 0002, employees 0006, training 0005+0006. Known: MFA and real-SMTP items in `REQUIRES_ATTENTION.md` are unchanged; framework mappings are indicative and need the compliance owner's confirmation; new staff accounts are still created in Django admin (no password handling in the console). **Email test-send, pre-flight checks, and landing-page cloning (2026-09-24)**: a saved email draft gets a "Checks" panel (`campaigns/email_checks.py` — spammy subjects, shouting, missing link/personalisation/plain-text part, image-heavy content; advisory only) and a "Send yourself a test" box that sends the actual composed message through the engine's default sending profile, subject prefixed `[TEST]` and every tracked link pointed at the portal's learn page instead of a live campaign; recipients are capped to the sender's own address or `TEST_EMAIL_ALLOWED_DOMAINS`, rate-limited per hour. `PhishingEngineClient.send_test_email` now takes an optional `template`/`url`. **Cloning a real login page** (Manage → Content → Landing pages → Clone a page): `campaigns/clone.py::validate_clone_url` blocks anything other than a public http(s) address (no credentials in the URL, no private/loopback/link-local/internal-looking host, DNS-resolved and rechecked — `LANDING_CLONE_ALLOW_PRIVATE_HOSTS` is dev-only); the page is fetched by the engine itself (`PhishingEngineClient.import_site`, Django has no route to the internet) and rebuilt by `sanitize_cloned_html` from a strict tag/attribute allow-list — no scripts/frames/objects/svg/event handlers/`javascript:`/unsafe CSS, every link inert, every form forced to post back to the landing page, password inputs kept native so `capture_passwords=False` still strips them (invariant #4). `LandingDraft.Layout.CLONED` stores `source_url`/`custom_html`; only the redirect URL is editable afterward, and "Fetch it again" re-runs the same validated fetch (re-checked every time, including on refetch, against DNS rebinding). 555 tests. A stray idle Postgres connection from an earlier interrupted session had been causing rotating, non-reproducible test failures across unrelated files — traced via `pg_stat_activity` (not assumed), cleared, confirmed with a from-scratch Postgres restart and one final clean serialized run.

## Non-negotiable invariants

These come from explicit decisions in the plan doc. Don't relitigate them without updating the plan doc first.

1. **Gophish is only ever called through a `PhishingEngineClient` adapter.** No code outside that adapter calls Gophish's REST API directly. This is what keeps the engine replaceable.
2. **Django and Gophish never share a database.** Gophish's own DB (MySQL) is never queried or joined by Django. Gophish IDs (`gophish_campaign_id`, etc.) are stored in Django as plain reference fields, not FKs.
3. **The event log is append-only and immutable.** Events are never updated or deleted — this is what makes retroactive risk-score recalculation possible. Webhook ingestion must be idempotent using Gophish's `external_id` (webhooks can be delivered more than once).
4. **Never persist real passwords.** Gophish landing pages use `capture_credentials=true, capture_passwords=false`. Defense in depth: webhook-ingestion code must also explicitly strip any password-like field before storing `metadata`, never rely solely on Gophish-side config. Any cloned/imported landing page template must be manually checked to confirm it uses a native `<form>` + `type="password"` input (a JS-based submission can bypass the stripping).
5. **`email_opened` is excluded from risk scoring** (or weighted ~0) — tracking-pixel prefetching by mail clients makes it unreliable as a behavior signal. Fine for delivery diagnostics only.
6. **Risk scores are versioned snapshots computed from the event log**, not a hardcoded formula (`employee_id, score, computed_at, algorithm_version, contributing_metrics`) — must be recomputable if the algorithm changes.
7. ~~Admin auth requires MFA~~ — **dropped for now, by explicit user decision (2026-09-22)**, not an oversight. Admin login is currently plain Django session auth with no MFA, even though an admin account can launch simulated attacks org-wide. Revisit before this platform handles anything beyond local dev/testing. Employee training login (not yet built) was planned to use SSO/OIDC via the company's IdP — that piece is unaffected by this decision.
8. **Network isolation is a hard requirement, not hardening.** Two Docker networks: `public` (Nginx + Gophish's phishing listener only) and `internal` (Django, Celery, Postgres, Redis, Gophish admin/API — no published ports). Driven by an unpatched HIGH-severity CVE (GO-2026-4455) that leaks the Gophish admin API key in rendered dashboard HTML; isolating the admin UI is the mitigation.
9. **Never use `:latest` for the Gophish image.** Pin an exact version/digest.
10. **Secrets (Gophish API key, webhook HMAC secret) live in the deployment's secret store**, never in code or committed config.
11. **No SPA.** Frontend is Django templates + HTMX + Chart.js — deliberately rejected React for this project's scale/traffic pattern.

## Tech stack (condensed — see plan doc for full rationale)

| Layer | Choice |
|---|---|
| Custom app | Django + Postgres |
| Phishing engine | Gophish (pinned version) + its own MySQL |
| Task queue | Celery + Celery Beat, Redis broker |
| Auth | Django session auth for now (MFA dropped — see invariant #7). OIDC/SSO for employees still planned, not built. |
| Frontend | Django templates + HTMX + Chart.js |
| Reverse proxy | Nginx + Let's Encrypt |
| Containerization | Docker Compose, single server |

## Open decisions (see plan doc "Overall Readiness Assessment")

1. Event log schema finalization — not yet resolved.
2. Credential-capture configuration sign-off — not yet resolved.
3. ~~Network isolation topology finalization~~ — **resolved and verified**. Implemented in `docker-compose.yml` exactly as invariant #8 describes; live-tested (see README.md "Verifying the network isolation").
4. Gophish version to pin (upstream vs. vetted fork, given GO-2026-4455) — **partially resolved**: pinned to upstream `v0.12.1`, built from source, confirmed it boots and migrates correctly (see "Known deployment quirks" below). The upstream-vs-fork provenance question itself is still open — revisit before any real campaign.
5. ~~Whether basic audit logging/RBAC moves into Phase 1~~ — **resolved**: yes, both landed in Phase 1 (`AuditLogEntry`/`log_action`, RBAC groups via `setup_groups`) and are exercised by Phase 2's training-assignment/reminder code too.

## Known deployment quirks (found by actually running the stack — see git history)

- **Gophish requires a relaxed MySQL `sql_mode`.** Its oldest DB migrations insert zero-dates (`'0000-00-00'`) that MySQL 8's default strict mode rejects outright, crashing Gophish on first boot. Fixed with `--sql-mode=NO_ENGINE_SUBSTITUTION` on the `mysql` service in `docker-compose.yml` — another concrete data point that upstream Gophish predates current tooling defaults (see open decision #4).
- **Docker Desktop does not forward host-published ports onto a network marked `internal: true`.** A container's `ports:` binding will show in `docker inspect`'s `HostConfig.PortBindings` but never actually listen. This is *why* `django` has no `ports:` entry in `docker-compose.yml` — verification goes through `docker compose exec <service> curl ...` instead, which also happens to match the real deployment's access pattern (VPN/tunnel, no published port) better than a host port would have.
- **Gophish's mail-sending worker can crash the whole process (Go panic) on a bad sending profile.** Confirmed live: created a real campaign end-to-end via `GophishClient.create_campaign()` (proving Django's side of "campaign sending" actually works against a real Gophish instance, not just the fake test client) — Gophish accepted it (201, `status: "In progress"`), then panicked (`index out of range [0] with length 0` in `mailer.go`) trying to send through a sending profile pointing at an unreachable SMTP host, taking the container down. The campaign creation itself is Django's responsibility and it worked correctly; the crash is upstream Gophish failing to handle an SMTP connection failure gracefully — another concrete data point for the Gophish maintenance-status risk. Mitigated with `restart: unless-stopped` on every long-running service in `docker-compose.yml` (none had a restart policy before this was found) — that bounds the downtime but doesn't fix the underlying fragility or un-stick a campaign left mid-send; a real deployment needs a working sending profile (real SES/SendGrid, per the plan's tech stack) and should still expect to monitor Gophish's own health.

## Project-specific subagents

`.claude/agents/` has seven subagents scoped to this stack: `backend-engineer`, `frontend-engineer`, `architecture-reviewer`, `database-engineer`, `security-code-reviewer`, `debugger`, `test-engineer`. Prefer them over the generic `claude`/`general-purpose` agent for work that clearly falls in their lane.

## Project-specific skills

`.claude/skills/` has detailed reference material each subagent loads for its lane — design tokens, schema shapes, conventions, checklists, playbooks — kept separate from the subagent prompts so those stay lean:

| Skill | Content |
|---|---|
| `frontend-design` | Type scale, spacing scale, color tokens, component patterns, HTMX conventions |
| `backend-conventions` | App layout, `PhishingEngineClient` interface shape, Celery/webhook conventions |
| `database-schema` | Concrete table shapes, constraints, indexing, migration rules |
| `code-review-checklist` | Itemized project-specific review checklist (credential handling, engine boundary, network isolation, etc.) |
| `debugging-playbook` | Symptom → likely cause → concrete command/query, for this stack's common failure modes |
| `testing-conventions` | Test layout, adapter mocking, required-coverage list |
